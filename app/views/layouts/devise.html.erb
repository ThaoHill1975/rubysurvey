<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Procurated Surveys - Sign In</title>
  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }
    body {
      font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
      -webkit-font-smoothing: antialiased;
    }
    .devise-container {
      min-height: 100vh;
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
      background-color: #f7fafc;
    }
    .devise-card {
      background: #fff;
      border-radius: 8px;
      padding: 40px;
      border: 1px solid #e2e8f0;
      max-width: 420px;
      width: 100%;
    }
    .devise-logo {
      text-align: center;
      margin-bottom: 32px;
    }
    .devise-logo img {
      height: 56px;
      width: auto;
    }
    .devise-card h2 {
      font-size: 20px;
      font-weight: 600;
      color: #1a202c;
      margin: 0 0 24px;
    }
    .devise-card .field {
      margin-bottom: 16px;
    }
    .devise-card label {
      display: block;
      font-size: 14px;
      font-weight: 500;
      color: #4a5568;
      margin-bottom: 6px;
    }
    .devise-card input[type="email"],
    .devise-card input[type="password"] {
      width: 100%;
      padding: 10px 12px;
      border: 1px solid #cbd5e0;
      border-radius: 6px;
      font-size: 14px;
      color: #2d3748;
      background: #fff;
    }
    .devise-card input[type="email"]:focus,
    .devise-card input[type="password"]:focus {
      outline: none;
      border-color: #3182ce;
      box-shadow: 0 0 0 3px rgba(49, 130, 206, 0.15);
    }
    .devise-card .actions {
      margin-top: 8px;
    }
    .devise-card .actions input[type="submit"] {
      width: 100%;
      padding: 10px;
      background: #3182ce;
      color: #fff;
      border: none;
      border-radius: 6px;
      font-size: 14px;
      font-weight: 500;
      cursor: pointer;
      transition: background-color 0.15s ease;
    }
    .devise-card .actions input[type="submit"]:hover {
      background: #2b6cb0;
    }
    .devise-card a {
      color: #3182ce;
      text-decoration: none;
      font-size: 13px;
    }
    .devise-card a:hover {
      text-decoration: underline;
    }
    .notice, .alert {
      padding: 10px 12px;
      border-radius: 6px;
      margin-bottom: 16px;
      font-size: 14px;
    }
    .notice { background: #c6f6d5; color: #276749; border: 1px solid #9ae6b4; }
    .alert { background: #fff5f5; color: #c53030; border: 1px solid #feb2b2; }
  </style>
</head>
<body>
  <div class="devise-container">
    <div class="devise-card">
      <div class="devise-logo">
        <img src="/images/procurated-logo.png" alt="Procurated">
      </div>
      <% if notice %><p class="notice"><%= notice %></p><% end %>
      <% if alert %><p class="alert"><%= alert %></p><% end %>
      <%= yield %>
    </div>
  </div>
</body>
</html>
